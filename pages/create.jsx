import React from 'react';
import OpenMetting from '../src/components/openMeeting';
import { Form } from 'antd';
const create = () => {
  return (
    <div style={{ width: '100%' }}>
      <OpenMetting />
    </div>
  );
};

export default create;
